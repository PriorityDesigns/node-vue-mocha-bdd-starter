'use strict';

import { shallowMount } from "@vue/test-utils";
import { expect } from "chai";

// Component or View under test and dependencies
// import x from y

describe("Component / View name - bdd tests", () => {
	let wrapper;

	beforeEach(() => {
		
	});

	describe("Feature - What is the feature under test", function() {
		describe("Scenario - A series of conditions and events that result in a testable outcome", function () {
			it("GIVEN a precondition is met");
			it("AND a another precondition");
			it("WHEN some action occurs");
			it("AND a another action occurs");
			it("THEN an expected outcome occurs");
			it("AND another expected outcome occurs");
		});
	})
	
});
